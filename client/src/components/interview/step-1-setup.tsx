export const Step1Setup = ({
  onStart,
}: {
  onStart: (data: {
    jobTitle: string;
    jobDescription: string;
    resumeUrl: string;
    experience: number;
    focusArea: string;
  }) => void;
}) => {
  return <div></div>;
};
