export const Step2Interview = ({
  interviewData,
  onComplete,
}: {
  interviewData: {
    jobTitle: string;
    jobDescription: string;
    resumeUrl: string;
    experience: number;
    focusArea: string;
  };
  onComplete: (report: any) => void;
}) => {
  return <div></div>;
};
